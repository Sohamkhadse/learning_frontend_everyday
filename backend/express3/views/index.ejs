<%- include("./head-foot/header.ejs")  %>

    <form method="POST" action="">
        <input type="text" placeholder="name" name="name">
        <input type="number" placeholder="age" name="age">
        <button type="submit">Submit</button>
    </form>

    <h1>myself <%= name %>   <h1>


        <% if (age<18){ %>
            <h1> i am smaller cause my age is <%= age %></h1>
        <% } %>

        <%  inputData.forEach((item)=>{ %>
            <h1>the data is , name <%= item.name %>, age is <%= item.age %>.</h1>
      <%  }) %>


    <%- include("./head-foot/footer.ejs")   %>